// Obsolete: DAO methods now take the Supabase client per call, so the DAO is
// context-neutral and this pre-bound browser singleton is no longer needed.
// Import the singleton from '@modules/product/dao/product.dao' instead. Safe to delete.
export {};
