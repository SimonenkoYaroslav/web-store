import en from '@components/ImageUpload/locales/en';

const { validation } = en.imageUpload;

export const IMAGE_FORMAT_MESSAGE = validation.formatMessage;
export const IMAGE_SIZE_MESSAGE = validation.sizeMessage;
export const IMAGE_DIMENSIONS_MESSAGE = validation.dimensionsMessage;
